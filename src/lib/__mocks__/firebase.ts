export const app = {}; // dummy app object
export const db = {}; // dummy db object
export const collection = jest.fn();
export const addDoc = jest.fn();
export const serverTimestamp = jest.fn(() => "MOCK_TIMESTAMP");
